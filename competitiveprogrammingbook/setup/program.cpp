#include <iostream>
using namespace std;
int main() {
    //make input output more efficient.
    ios::sync_with_stdio(0);
    cin.tie(0);

    int a, b;
    string x;
    cin >> a >> b >> x;
    cout << a << " " << b << " " << x << "\n";
}